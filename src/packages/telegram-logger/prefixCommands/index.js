import logger from '../../../utils/logger.js';

// This package doesn't currently use Discord prefix commands
// This file is a placeholder for potential future prefix commands

export function registerPrefixCommands(client, config) {
  logger.debug('No prefix commands to register for telegram-logger package');
}
