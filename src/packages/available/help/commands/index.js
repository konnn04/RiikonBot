import logger from '../../../../utils/logger.js';

export function registerCommands(client) {
  // Commands are registered in clientReady event
  logger.debug('Help commands structure initialized');
}
