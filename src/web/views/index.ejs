<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>RiikonBot - Discord Bot</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container mt-5">
    <div class="row">
      <div class="col-md-8 offset-md-2 text-center">
        <h1 class="mb-4">RiikonBot</h1>
        <p class="lead">A modular Discord bot with extensible package management</p>
        
        <div class="mt-5">
          <% if (isAuthenticated) { %>
            <div class="card mb-4">
              <div class="card-body">
                <div class="d-flex align-items-center">
                  <% if (user.avatar) { %>
                    <img src="https://cdn.discordapp.com/avatars/<%= user.discordId %>/<%= user.avatar %>.png" 
                         class="rounded-circle me-3" width="50" height="50" alt="Avatar">
                  <% } else { %>
                    <div class="rounded-circle me-3 bg-secondary d-flex align-items-center justify-content-center" 
                         style="width: 50px; height: 50px; color: white;">
                      <%= user.username.charAt(0).toUpperCase() %>
                    </div>
                  <% } %>
                  <div>
                    <h5 class="card-title mb-0">Welcome, <%= user.username %></h5>
                    <p class="text-muted mb-0"><%= user.isAdmin ? 'Administrator' : 'User' %></p>
                  </div>
                </div>
              </div>
            </div>
            <a href="/dashboard" class="btn btn-primary btn-lg me-2">Go to Dashboard</a>
            <a href="/auth/logout" class="btn btn-outline-secondary btn-lg">Logout</a>
          <% } else { %>
            <a href="/auth/login" class="btn btn-primary btn-lg">Login with Discord</a>
          <% } %>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
